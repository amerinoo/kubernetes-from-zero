apiVersion: v1
kind: Service
metadata:
  name: whoami
  labels:
    app: whoami
    environment: {{ .Values.env | quote }}
spec:
  selector:
    app: whoami
    environment: {{ .Values.env | quote }}
  ports:
    - port: 80
      targetPort: 80
