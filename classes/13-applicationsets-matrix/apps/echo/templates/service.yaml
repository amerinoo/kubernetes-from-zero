apiVersion: v1
kind: Service
metadata:
  name: echo
  labels:
    app: echo
    environment: {{ .Values.env | quote }}
spec:
  selector:
    app: echo
    environment: {{ .Values.env | quote }}
  ports:
    - port: 80
      targetPort: 80
