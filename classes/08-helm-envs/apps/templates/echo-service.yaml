apiVersion: v1
kind: Service
metadata:
  name: echo
  labels:
    environment: {{ .Values.environment | quote }}
spec:
  selector:
    app: echo
    environment: {{ .Values.environment | quote }}
  ports:
    - port: 80
      targetPort: 80
