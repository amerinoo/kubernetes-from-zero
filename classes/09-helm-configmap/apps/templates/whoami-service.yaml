apiVersion: v1
kind: Service
metadata:
  name: whoami
  labels:
    environment: {{ .Values.environment | quote }}
spec:
  selector:
    app: whoami
    environment: {{ .Values.environment | quote }}
  ports:
    - port: 80
      targetPort: 80
